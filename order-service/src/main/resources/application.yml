server:
  port: 8082
  error:
    include-message: always

spring:
  application:
    name: order-service
  cloud:
    openfeign:
      circuitbreaker:
        enabled: true
  # orders_db
  datasource:
    url: jdbc:postgresql://localhost:5432/orders_db
    username: postgres
    password: qweryt123
  # Hibernate
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
  # RabbitMQ
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

# (User Service)
user:
  service:
    url: http://localhost:8081

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${server.port}

management:
  endpoint:
    health:
      show-details: always
  health:
    # Если падает из-за DB/RabbitMQ в режиме отладки:
    db:
      enabled: false

logging:
  level:
    org:
      hibernate: ERROR
      hibernate.SQL: OFF
      springframework:
        web: DEBUG
        security: DEBUG

feign:
  circuitbreaker:
    enabled: true

resilience4j:
  circuitbreaker:
    configs:
      default:
        slidingWindowType: COUNT_BASED
        slidingWindowSize: 5
        minimumNumberOfCalls: 3               # Начинаем вычислять % ошибок после 3 вызовов
        failureRateThreshold: 50              # Если >= 50% ошибок -> переводим в OPEN
        waitDurationInOpenState: 10000ms      # Находимся в OPEN 10 секунд
        permittedNumberOfCallsInHalfOpenState: 2
        recordExceptions:
          - org.springframework.web.server.ResponseStatusException # ◄ Добавьте класс исключения, которое кидает ваш CustomFeignErrorDecoder
          - feign.FeignException
    instances:
      user-service:
        slidingWindowSize: 5
        minimumNumberOfCalls: 3
        failureRateThreshold: 50              # Отключаем при 50% ошибок
        waitDurationInOpenState: 10000ms
        recordExceptions:
          - com.quickbite.orderservice.exception.UserServiceException